# GCP Project ID where the resources will be deployed
project_id  = "your-gcp-project-id"

# Globally unique name for the Google Cloud Storage bucket (e.g. kamloops-gtfs-data)
bucket_name = "your-globally-unique-bucket-name"

# GCP region to deploy resources (defaults to us-central1)
region      = "us-central1"
